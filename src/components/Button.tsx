import { memo } from "react";
import { Link } from "../tools/link";


export type ButtonProps = Link;




export const Button = memo((props: ButtonProps) => {
    //const {href, label, onClick} = props;
    return <button>

    </button>
})